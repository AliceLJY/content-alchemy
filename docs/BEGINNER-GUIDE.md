# Content Alchemy 新手补充指南

> 👋 **完全不懂技术？没关系！这份指南专门为你准备。**

---

## 🎯 这份指南是给谁看的？

如果你：
- 从来没用过"终端"这个黑色窗口
- 不知道什么是"命令行"
- 看到代码就头疼
- 只想快速上手，不想了解技术细节

**那么这份指南就是为你准备的！**

---

## 📚 Part 1: 基础概念（5分钟读完）

### 什么是"终端"？

**终端（Terminal）** 就是Mac自带的一个黑色窗口程序。

**怎么打开？**
1. 按 `Command + 空格` 打开聚焦搜索
2. 输入"终端"或"Terminal"
3. 按回车

**它长什么样？**
```
Last login: Sat Jan 24 10:30:00 on ttys001
alice@MacBook-Air ~ %
```

那个闪烁的光标就是等你输入命令的地方。

---

### 什么是"命令"？

**命令** 就是用文字告诉电脑做什么。

**举例：**
- `ls` = 看看这个文件夹里有什么文件
- `cd Documents` = 进入"文档"文件夹
- `pwd` = 告诉我现在在哪个文件夹

**怎么执行命令？**
1. 把命令复制粘贴到终端
2. 按 `Enter` 键
3. 等待结果

---

### 重要符号说明

| 符号 | 意思 | 示例 |
|------|------|------|
| `~` | 你的用户文件夹 | `~/Documents` = 你的文档文件夹 |
| `/` | 文件夹分隔符 | `~/Desktop/test.txt` |
| `$` | 提示符（不要复制） | `$ ls` 你只复制 `ls` |
| `#` | 注释（不会执行） | `# 这是说明` |
| `\` | 转义符（保留空格） | `Google\ Chrome` |

---

### 引号的坑（重要！）

**错误示例：**
```
pkill "Google Chrome"  ← 中文引号，会报错！
```

**正确示例：**
```
pkill "Google Chrome"  ← 英文引号，正确！
```

**怎么避免？**
- 方法1：从文档直接复制粘贴（不要手打）
- 方法2：检查引号是直的 `"` 还是弯的 `"`（弯的是错的）

---

## 🚀 Part 2: 安装流程图解

### 总览：你需要做什么？

```
第1步：安装Bun
   ↓
第2步：下载Content Alchemy
   ↓
第3步：下载Baoyu工具
   ↓
第4步：配置Chrome
   ↓
第5步：测试运行
```

**总耗时：约15-20分钟**

---

### 第1步：安装Bun（5分钟）

**Bun是什么？**
一个运行JavaScript代码的工具（你不需要理解，照做就行）。

**操作步骤：**

1. 打开终端

2. 复制这个命令，粘贴到终端，按回车：
   ```bash
   curl -fsSL https://bun.sh/install | bash
   ```

3. 等待安装（会看到进度条，大约1-2分钟）

4. **关键步骤！** 安装完成后，**完全关闭终端窗口**（点左上角红色按钮）

5. 重新打开终端

6. 验证安装成功：
   ```bash
   bun --version
   ```
   
   **应该看到：** `1.0.25`（或其他数字）
   
   **如果看到：** `command not found` → 回到第4步，确保关闭并重开了终端

---

### 第2步：下载Content Alchemy（3分钟）

**操作步骤：**

1. 确保终端是打开的

2. 进入文档文件夹：
   ```bash
   cd ~/Documents
   ```

3. 下载项目：
   ```bash
   git clone https://github.com/AliceLJY/content-alchemy.git
   ```
   
   **会看到：** `Cloning into 'content-alchemy'...`（下载中）

4. 进入项目文件夹：
   ```bash
   cd content-alchemy
   ```

5. 验证成功：
   ```bash
   ls
   ```
   
   **应该看到：** `README.md  SETUP.md  SKILL.md`等文件

---

### 第3步：下载Baoyu工具（2分钟）

**这是什么？**
用来自动发布到微信公众号的工具。

**操作步骤：**

1. 确保你在 `content-alchemy` 文件夹里
   ```bash
   pwd
   ```
   
   **应该看到：** `/Users/你的用户名/Documents/content-alchemy`

2. 下载Baoyu工具：
   ```bash
   git clone https://github.com/JimLiu/baoyu-skills.git dependencies/baoyu-skills
   ```

3. 验证成功：
   ```bash
   ls dependencies/baoyu-skills/skills/baoyu-post-to-wechat/scripts/
   ```
   
   **应该看到：** `wechat-article.ts`

---

### 第4步：配置Chrome（5分钟）

**为什么需要这步？**
因为自动发布需要控制Chrome浏览器，需要特殊方式启动。

**操作步骤：**

1. 完全关闭Chrome（如果打开着的话）
   ```bash
   pkill "Google Chrome"
   ```
   
   **注意：** 这个命令没有输出是正常的

2. 等待3秒（让系统清理干净）

3. 以特殊模式启动Chrome：
   ```bash
   /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 &
   ```
   
   **会看到：** Chrome自动打开，顶部有黄色警告条"正在调试此浏览器"（这是正常的）

4. 验证端口开启：
   ```bash
   lsof -i :9222
   ```
   
   **应该看到：** 一行包含 `Google` 和 `9222` 的内容
   
   **如果什么都没看到：** 回到步骤1重新操作

---

### 第5步：测试运行（5分钟）

**操作步骤：**

1. 创建测试文章：
   ```bash
   cat > ~/Documents/test.md << 'EOF'
   # 测试文章
   
   这是测试内容。
   
   本文由Content Alchemy自动生成。
   EOF
   ```

2. 在Chrome中登录微信公众平台
   - 访问：https://mp.weixin.qq.com
   - 扫码登录
   - 保持Chrome打开（可以最小化）

3. 打开Antigravity，输入：
   ```
   帮我把 ~/Documents/test.md 发布到微信公众号草稿箱。
   
   使用项目：~/Documents/content-alchemy
   Baoyu脚本路径：dependencies/baoyu-skills/skills/baoyu-post-to-wechat/scripts/wechat-article.ts
   ```

4. 等待Antigravity执行

5. 在微信后台查看草稿箱，应该能看到测试文章

**成功了？** 🎉 恭喜！你已经完成安装！

**失败了？** 👇 继续看下面的排错指南

---

## 🆘 Part 3: 常见问题排查

### 问题1：Bun提示"command not found"

**原因：** 没有重启终端，或者路径没加载

**解决方法：**
```bash
# 方法1：手动加载
source ~/.zshrc

# 方法2：彻底重启
# 关闭终端窗口，重新打开

# 方法3：检查是否真的安装了
ls ~/.bun/bin/bun
# 如果有输出，说明安装成功但路径没加载，用方法1或2
```

---

### 问题2：Git提示"command not found"

**原因：** Mac没有安装Git

**解决方法：**
```bash
# 执行这个命令，Mac会自动弹出安装提示
git --version

# 点击"安装"按钮，等待安装完成
```

---

### 问题3：Chrome启动命令报错

**常见错误：** `No such file or directory`

**原因：** Chrome路径不对，或者引号是中文的

**解决方法：**
```bash
# 方法1：检查Chrome是否安装
ls /Applications/ | grep Chrome

# 如果没有"Google Chrome.app"，去官网下载安装
# https://www.google.com/chrome/

# 方法2：检查引号
# 确保命令中的引号是英文的 " 而不是中文的 "
```

---

### 问题4：lsof检查端口时没有输出

**原因：** Chrome没有正确启动

**解决方法：**
```bash
# 1. 再次杀掉Chrome进程
pkill -9 "Google Chrome"

# 2. 等待5秒
sleep 5

# 3. 重新启动
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 &

# 4. 等待Chrome窗口出现后，再检查
lsof -i :9222
```

---

### 问题5：Antigravity执行失败

**可能原因和解决方法：**

1. **Chrome没开调试端口**
   ```bash
   lsof -i :9222  # 检查端口
   # 如果没输出，重新启动Chrome
   ```

2. **没登录微信公众平台**
   - 打开Chrome
   - 访问 https://mp.weixin.qq.com
   - 扫码登录

3. **路径错误**
   - 确保test.md在 ~/Documents/ 下
   - 确保content-alchemy在 ~/Documents/ 下

---

### 问题6：草稿保存了但格式乱了

**常见情况：**
- 段落合并在一起
- 引号变了
- 图片位置不对

**解决方法：**
- 这是Markdown转微信格式的正常现象
- 在微信编辑器里手动调整一下格式
- 设置封面图
- 然后发布

---

## 🎓 Part 4: 日常使用流程

### 每次使用前（必做）

```bash
# 1. 启动Chrome调试模式
chrome-debug

# 2. 在Chrome中登录微信公众平台（如果还没登录）

# 3. 保持Chrome打开

# 4. 开始使用Antigravity创作
```

### 快捷命令设置（可选但推荐）

如果每次都要输入那么长的Chrome启动命令太烦，可以设置快捷命令：

```bash
# 运行安装脚本
cd ~/Documents/content-alchemy
./scripts/setup.sh

# 之后只需要输入：
chrome-debug
# 就能启动调试模式的Chrome了！
```

---

## 💡 Part 5: 小技巧

### 技巧1：终端复制粘贴

- **复制：** `Command + C`
- **粘贴：** `Command + V`
- 和普通程序一样！

### 技巧2：快速清屏

如果终端里内容太多看着乱：
```bash
clear
```
或者按 `Command + K`

### 技巧3：查看命令历史

按 `↑`（向上箭头）可以查看之前执行过的命令

### 技巧4：Tab自动补全

输入文件名的前几个字母后按 `Tab` 键，会自动补全

例如：
```bash
cd ~/Doc[按Tab键]
# 自动变成：
cd ~/Documents/
```

### 技巧5：取消正在执行的命令

如果命令卡住了，按 `Control + C` 可以强制停止

---

## 📞 Part 6: 获取帮助

### 自动检查环境

如果不确定哪里出问题了：

```bash
cd ~/Documents/content-alchemy
./scripts/doctor.sh
```

这个脚本会自动检查所有配置，告诉你哪里有问题。

### 检查清单

手动检查每一项：

- [ ] Bun已安装：`bun --version` 有输出
- [ ] 项目已下载：`ls ~/Documents/content-alchemy` 有内容
- [ ] Baoyu已下载：`ls dependencies/baoyu-skills` 有内容
- [ ] Chrome端口已开：`lsof -i :9222` 有输出
- [ ] 已登录微信：Chrome中打开 https://mp.weixin.qq.com

全部打钩？那就可以开始使用了！

---

## 🎉 结语

**恭喜你读到这里！**

虽然这些技术概念一开始可能有点陌生，但只要跟着步骤做，其实并不难。

记住：
- 遇到问题很正常，不要慌张
- 仔细检查引号、路径、是否在正确的目录
- 善用 `doctor.sh` 诊断工具
- 实在不行，从头再来一遍（删掉重新克隆）

**你可以的！** 💪

---

**问题反馈：**
如果这份指南没有解决你的问题，请在GitHub提Issue：
https://github.com/AliceLJY/content-alchemy/issues
